# TRACKER — Scansia Manager (refactor Sync-Scansia → web utility interna)

## Current state

**Fase**: F1 (MVP) — inizio esecuzione. Branch `feat/scansia-manager`.
**Piano approvato**: `~/.claude/plans/compressed-foraging-stream.md` (327 righe, 9 giri di critica avversariale, F1≈8 execution-ready; F2 reorder = ciclo di pianificazione dedicato successivo).

**Milestone F1** (critical path `M1a→M1b→[handoff SERVER+shared-secrets]→M2→M3→M4→M5→M-docs`):
- [x] **M1a-lib** — *(layer libreria + 8 fix COMPLETO)* `transport.py` (unificato, fix timeout, retry) · `shopify/ops.py` (+net-new) · `gsheet/` · `services/resolvers.py` · `services/outlet_service.py` (orchestrazione publish CREATE/ACTIVE/DRAFT). **Fix**: 1 (cleanup tutte location≠Promo) ✅ · 2 (DRY_RUN fail-closed main.py) ✅ · 3 (userErrors→raise) ✅ · 4 (blocco prezzo 0/≥compareAt) ✅ · 5 (size-match Size+SKU cross-check) ✅ · 7 (media→productSet) ✅ · **6 (tag) e 8 (batching) = gated/F2, non M1a**. Review avversariale su ogni layer; 2 HIGH phantom-stock/oversell intercettati e corretti (duplicato DRAFT-atomico; DRAFT-revive quarantena). **129 test verdi.**
- [~] **M1a-consolidate** — ✅ **GSheet layer** `backend/gsheet/` (append-oriented, row_uuid+reconciled, `backfill_cutover` idempotente anti-re-inflate VERIFICATO, write-back per row_uuid+CAS, `parse_qta`, `eligible_rows` parità legacy, DRY_RUN-safe read). Review avversariale PASS. ✅ `docs/business-context.md` (ground-truth verificata: 3 collezioni smart, loop publish→SALDI→reorder, Q-bf-storefront, oversell 38 CONTINUE, segnale rientro, downstream feed). ✅ cleanup moduli morti (`src/gsheets.py`, `src/utils.py`, `reorder/` — verificato zero import esterni prima di rimuovere; `src/exceptions.py` era già assente). ⏭ resta SOLO: regression checkpoint CLI-fixato-vs-prod (richiede store live → deferito post-credenziali). Orfani deps `pandas`/`slugify`/`tenacity` (solo in `utils.py` rimosso) → rimozione da requirements deferita (tocca build; `slugify` rientra a F2).
- [~] **M1b** — ✅ **codice** op net-new (`read_variant_inventory` 0-vs-assente + cap-guard via `hasNextPage`, `product_delete`, `get_online_store_publication_id`+`product_publish` via `publishablePublish`, `enumerate_outlet_products` paginata) + `services/resolvers.py` (outlet/source, SKU-non-univoco: dedup per gid, multi-match→warning, mai outlet-come-source). Review verificata su doc 2025-07, 107 test. ✅ **VERIFICA LIVE READ-ONLY 2026-07-07** (shopify-operator, store reale API 2025-07, 0 mutazioni): inventory-read schema **PASS** (`available`/`committed`/`on_hand`, 0-vs-nodo-assente distinguibili, no troncamento — max **3** nodi/variante su 12.712, `hasNextPage`=0); enumeration OUTLET **182==182** (membership==wildcard, diff 0) + case-insensitive + `title:Outlet` senza wildcard=0; resolver SKU-non-univoco confermato (2/3 SKU → outlet+full-price); **`sku=NULL` GIÀ gestito nel codice** (resolvers `(sku or "")` guard + exact-verify scarta null-match; size-match size-primary). Ground-truth **tutte le ancore combaciano** (OUTLET 182=131/51/0, SALDI 697=670/20/7, BF 182, Promo `61184966721` ok, **38 CONTINUE** =33/5, Promo 133 / solo-Magazzino 0). ✅ orphan-handle diff **0 orfani** (167 handle-`-outlet` ⊆ 182 OUTLET; handle affidabile solo 91,8% → conferma design: enumeration via membership/titolo, mai handle). **VERIFICA LIVE READ-ONLY COMPLETA.** ⏭ solo productDuplicate→auto-publish Online Store differito al 1° APPLY (è mutazione).
- [x] **M2 COMPLETO** — *(foundation web + UI + DEPLOY)* `persistence/ports.py` (Protocol `AuditSink`/`TokenService` = contratto porting) · `tokens.py` (confirm-token HMAC stdlib, fail-closed) · `gsheet_audit.py` (audit su tab GSheet, `write_durable` gate-abort delete verificato + header-repair) · `auth/basic_auth.py` (Basic Auth fail-closed constant-time) · `app.py` (FastAPI, mutex single-slot + ThreadPoolExecutor off-loop, error-boundary no-leak, `/health` non gated) · `api/` read (`/scansia` + job inventario con freschezza stale/failed, `/audit`) · **UI dashboard** `api/ui.py` + `static/{index.html,app.js,app.css}` (vanilla JS zero-dip, tab Scansia/Publish/Prezzi/Delete/Audit, chip+freschezza, flusso preview→conferma→apply che trasporta plan_hash+confirm_token, gesto CONFERMO+conteggio per il delete, tutto gated Basic Auth, zero secret nel client). Review 3-lenti: 0 high/critical. ✅ **DEPLOYATO LIVE 2026-07-07**: https://scansia-manager.onrender.com (Render **free**, branch `feat/scansia-manager`, Basic Auth attiva `GET /`→401, `/health`→200 boot pulito). Python pin via `.python-version=3.12.4` (Render ignora `runtime.txt`; env `PYTHON_VERSION` manuale rimosso da SERVER). Handoff M2-server **CHIUSO**.
- [~] **M3** — *(endpoint publish COMPLETI)* `api/publish.py` preview/apply (wrappa `outlet_service`, confirm-token HMAC, drift-gate `VERIFY_FAILED`, audit+actor). Review: confirm-gate 0 difetti. **AUTOMATE handoff VERIFICATO LIVE 2026-07-08** (make-operator vs Make REST API, scenari `2215567` return-append + `678434` order — è l'account Racoon, ispezionato diretto; tutte le 5 Q §7 risolte; `docs/handoff-M3-automate.md` §7-§10). **Esiti/analisi SVILUPPO (§10)**: (a) **§9 race secondo-writer NEUTRALIZZATA** — il tool non fa NESSUNA op su righe (solo `update_cell`+colonne a destra, verificato grep), row-number stabili → `678434` (updateRow per numero-riga) non può rompersi; colonne disgiunte; il tool non deriva mai stock da `Qta`. Invariante load-bearing: "il tool non inserisce/cancella/riordina righe". (b) **`online` già gestito** — `mark_deleted` scrive campo parametrizzato (CI-6), non hardcoded → adottare `_scansia_status=deleted`. **Fixed-index append RISOLTO 2026-07-08** (2° pass make-operator): era un MISREAD — `2215567` module 15 ha `includesHeaders:true` = header-name stable-id, già robusto ad add/remove/reorder colonne (rompe solo su RENAME header); niente da cambiare in Make. §9 race neutralizzata (§10). `online` delete→`_scansia_status` (codice già parametrizzato CI-6). **Q "Prezzo Outlet"(idx 9) SKIPPATA** su decisione owner (vuole provare la GUI in produzione prima). ✅ **INIZIALIZZA COSTRUITA 2026-07-09** (commit `3f0617c`; spec `docs/init-reconcile-design.md`): banner GUI di primo avvio (niente admin URL separato) → **part A** cutover baseline (row_uuid+reconciled+sentinel, anti-raddoppio) + **part B** reconcile online-flag: per ogni riga `online=si`, "davvero online" = outlet esiste ∧ ACTIVE ∧ variante di quella taglia `available>0` su Promo; altrimenti **DRAFT** del prodotto (se esiste) + `online=NO` + `Vendute il`=timestamp UTC ISO (`...T...:...:...000Z`, owner-confermato, = formato `created_at` di Make); multi-match→review, missing→solo foglio. preview→conferma→apply Basic-Auth+confirm-token + snapshot durevole **AUDIT_INIT** abort-on-failure (come delete). Build via workflow Design→Implement→Review; **2 HIGH+5 LOW intercettati e corretti pre-commit** (size normalizzata nel plan_hash; before-snapshot gate; isolamento per-SKU; catch DRAFT allargato; GUI gesture_required; test available-vs-on_hand). 283 test stdlib / 41 init in venv verdi. **Sblocca la GUI in produzione** (era fail-closed sul cutover). ⏭ resta (LIVE): **push branch + deploy SERVER** (autoDeploy OFF) → Ale lancia Inizializza dalla GUI; + decommissiona cron (dashboard) + gate Q-bf-storefront + il flusso Make return-signal ONGOING (rientri freschi, `handoff-M3-automate.md`).
- [~] **M4** — *(libreria + endpoint COMPLETI)* `services/pricing_service.py` (3 modalità, validazione price≤0/≥compareAt, eligibility+ACTIVE-only, fill-missing RIFIUTA, revert-capture) + `api/prices.py` preview/apply/revert/discharge-debt (confirm-token). ⏭ **discharge-debt recon 2026-07-07 = N=0**: sui 131 OUTLET ACTIVE (1.596 varianti) **0** con `price==0` o `price≥compareAt` (margini sconto min 9,6% / mediana 21,2% / max 58,4% — prezzi già sani). Il debito atteso è **VUOTO** → l'acceptance "ripara N rotti sullo store" non ha dati su cui girare. **Ridefinizione**: il valore del modulo prezzi è la **gestione ongoing**, non il cleanup una-tantum → acceptance = **test controllato/sintetico al 1° APPLY** (imposta uno sconto su 1 outlet ACTIVE via modalità 1/2 → verifica live → revert), non dipendente da dati rotti preesistenti. [FINDING per owner]
- [~] **M5** — *(libreria + endpoint COMPLETI)* `services/delete_service.py` (predicato per-variante esatto; `available is None`→REVIEW; snapshot durevole OBBLIGATORIO pre-delete abort-on-failure; gesto umano; hard cap 50) + `api/delete_outlet.py` zero-stock/cleanup/single/deny-normalize. **Hardening**: single-delete rifiuta gid non-outlet; `second_confirm` legato al piano fresco; count vincolato al set (drift→VERIFY_FAILED). Review delete-safety: gate NON aggirabile, 0 high/critical. ⏭ resta (LIVE): delete singolo validato → poi cleanup bulk; `urlRedirectCreate` follow-up.
- [ ] **M-docs** (STAND-BY fino a go-live: descrive stato operativo non ancora attivo) — runbook operatore + rewrite README (1011 righe, legacy cron-centric) + ~~ritiro FIX_PRICES_README~~ ✅ **cancellato 2026-07-07** (bug chiuso, discharge-debt=0; aveva una traduzione EN WIP scartata su decisione owner) + cross-doc **outer-repo** (system-map, dependencies-graph, shared-secrets — cross-boundary, da segnalare prima) + rewrite CLAUDE.md progetto (**ora tracked**, ancora identità legacy → web-utility a go-live). `.gitignore` (+`.DS_Store`) e CLAUDE.md progetto ora committati.

**Handoff (OPEN-HANDOFFS.md ripulito da SERVER — entrambe le righe M2 rimosse)**:
- **~~SERVER deploy~~ CHIUSO** (2026-07-07): service Render free deployato+live, env var settate (secret riusati + `APP_PASSWORD`/`TOKEN_SIGNING_SECRET`), no keepalive (cold-start ~30-60s accettato). **Trigger residui verso SERVER**: (a) autoDeploy=**OFF** (= deploy-gate owner) → a ogni nuovo push su `feat/scansia-manager` avvisare SERVER (o Manual Deploy da dashboard); (b) al **merge M2→main** avvisare SERVER per ri-puntare il branch del service a `main`. Brief: `docs/handoff-M2-server.md`.
- **~~shared-secrets~~ RISOLTO** (SERVER-verified): riuso token "Management esterno" (8/8 scope) → no app dedicata; riuso SA Sync-Scansia → no SA dedicata; no rotazione. Brief: `docs/handoff-M2-shared-secrets.md`.
- **AUTOMATE** (pre-M3, verificato live 2026-07-08, 2 decisioni residue per Ale): integrazione flusso Make return-signal. Brief `docs/handoff-M3-automate.md` **RISCRITTO 2026-07-07**, poi **VERIFICATO LIVE 2026-07-08** via `make-operator` (accesso REST API diretto a Make, nuovo ruolo standing — v. `IT/AUTOMATE/CLAUDE.md`): tutte le 5 domande aperte §7 risolte con evidenza sui blueprint reali (non serviva "chiedere al Make owner" — è l'account stesso di Racoon). Esito: **`online` è il flag legacy di stock-availability** (mai riusarlo per delete write-back, va bene la colonna privata già prevista) · **append per INDICE FISSO confermato**, non per header name (oggi allineato per caso, nessun rilevamento drift lato tool) · **`row_uuid`/`reconciled` safe a indice 18+** (nessuno dei due writer Make scrive oltre indice 14). **2 NUOVI item bloccanti per Ale**: (1) **secondo scrittore concorrente scoperto** — lo scenario `678434` "Shopify - Nuovo Ordine" decrementa `Qta`/flippa `online`/logga l'ordine sulla stessa riga **by row-number, no CAS**, ogni 15 min, per le righe outlet-matched (le righe non-outlet passano invece da MySQL, nessuna doppia contabilità lì — mia correzione dopo un primo draft impreciso, grazie ad Ale per il controllo) → race reale contro le mutazioni row_uuid del tool, serve una decisione di sequencing prima del go-live M3; (2) colonna **"Prezzo Outlet" (indice 9) non scritta da nessuno dei due scenari Make** — capire chi la popola, se qualcuno. Dettaglio completo §9 del brief.

**Domande owner aperte** (tracciate nel piano §Open items): Q-oversell (default DENY), Q-bf-storefront (gate M3), Q-prodcollection, Q-modelsrc, Q-nest, Q-nonfootwear, Q-saleschannel (verifico io in M1b).

**Decisioni owner risolte**: hard-delete · titolo=sorgente · motore reorder D/B/A B-over-model · MVP-first · Q-active/freshreturn=**Make append-row = segnale rientro, delta idempotente reconciled, PULL** · Q-roweligibility=filtro legacy `online=SI AND qta>0` · Q-fixprices=rifiuta fill-missing · **Q-persistenza (2026-07-06) = Option A "no DB, sheet-centric"** (supera SQLite-on-disk: disco Render a pagamento escluso): confirm-token firmati HMAC stateless, audit + `before_snapshot` delete su tab GSheet, piani ricalcolati all'apply → **Render FREE ok**. Storage dietro Protocol iniettati (`AuditSink`/`TokenService`) → porting a MySQL domani = 1 adapter, no redesign. 2ª sede durevole delete opzionale = Cloudflare R2 (free). · **Q-auth (2026-07-06) = HTTP Basic Auth app-level, NESSUN perimetro di rete** (Explore+SERVER: nessun tool interno Racoon usa CF/Tailscale/VPN/IP-allowlist; CF non è nemmeno nello stack, DNS Aruba). `APP_PASSWORD` constant-time fail-closed, come Manager_Console. Eliminati CF Access/JWKS/origin-lockdown/`CF_ACCESS_AUD`/dominio custom. **Secret riusati** (SERVER-verified): token "Management esterno" 8/8 scope → no app dedicata; SA Sync-Scansia → no SA dedicata; no rotazione. Safety delete resta app-level.

**F1 BUILD COMPLETO + M2 LIVE + VERIFICA READ-ONLY PASSATA + INIZIALIZZA COSTRUITA + PUSHATA.** Store reale validato (recon 2026-07-07: 5/6 PASS, discharge-debt VUOTO N=0). L'Inizializza (commit `3f0617c`) sblocca la GUI in produzione. **Push completato 2026-07-09**: `origin/main` e `origin/feat/scansia-manager` entrambi a `9270741` (fast-forward, tutta F1 + Inizializza — verificato via `git fetch`). **Handoff SERVER aperto 2026-07-09** (`docs/handoff-deploy-check-server.md`, riga in `IT/OPEN-HANDOFFS.md`): serve riconciliare branch/autoDeploy del service Render (era `feat/scansia-manager`/autoDeploy OFF, ora Ale vuole deploy automatico da `main`) + conferma boot pulito di `9270741` (`/health`=200, Basic Auth attiva). **Prossimo (nell'ordine)**: (1) **attesa SERVER** su riconciliazione branch/autoDeploy + conferma deploy live → **Ale lancia Inizializza dalla GUI in produzione** (preview→conferma) e prova il controllo manuale online/offline per-prodotto. (2) **go-live mutazioni** dietro deploy-gate: 1° APPLY controllato (delete singolo → cleanup bulk; prezzi = test sintetico set+revert, N=0). (3) **publish-live** = ancora BLOCCATO sul flusso Make return-signal ONGOING (rientri freschi; `handoff-M3-automate.md` — Q2 ri-verificata esaustivamente 2026-07-09: 2 writer live confermati, 3° scenario `4100540` è duplicato dormiente, non un rischio) + Q-bf-storefront + decommission cron. **Decisione owner**: discharge-debt=0 → acceptance M4 ridefinita (riga M4). shared-secrets + SERVER deploy(M2) CHIUSI.

**Known follow-up** (non bloccanti): (1) fallimento di `read_variant_inventory` DOPO `productDuplicate` in CREATE → **duplicato DRAFT orfano** (il resolver lo ri-aggancia al run successivo; TODO reconcile per-titolo). (2) delete: `urlRedirectCreate` (404/SEO su delete ACTIVE) non implementato (nessun op) → runbook M-docs. (3) delete drift a re-verify → abort dell'INTERO batch (più stretto di esclusione per-item; il conteggio umano è vincolato al set). (4) `delete_single_apply` non ri-applica il predicato zero-stock (by design). (5) `gsheet_audit.capture_before` ha lo stesso pattern serialize-pre-append di `write_durable` — gap non-`SheetIOError` benigno (il layer pricing cattura `Exception`); allineare per consistenza. (6) test FastAPI-gated skippano senza `fastapi`/`httpx` nell'interprete (banner nei file); girano su Render/venv — full suite ~393 con Inizializza. (7) **[da Inizializla review]** il gap GUI `gesture_required` (jobFailureMessage ritornava null → crash a valle) è stato corretto SOLO in `initApply`; l'helper è condiviso → **verificare se publish/cleanup/prezzi hanno lo stesso latente** e allineare. (8) `AuditSink` Protocol (`ports.py`) NON include `write_init_before` (sink duck-typed, nessun isinstance rotto) — se un domani si vuole il Protocol come unica fonte per tutti i sink, aggiungerlo.

**Stato working tree**: backend F1 completo. `backend/{config,shopify,gsheet,services,persistence,auth,api(read+3 verticali),app.py}` + tests + requirements. **283 test locali (stdlib, +110 skip FastAPI) / ~393 in venv** (con Inizializza). ⚠ `docs/testing.md`: la full suite richiede fastapi/httpx nel venv. **✅ Pushato 2026-07-09: `origin/main` e `origin/feat/scansia-manager` entrambi a `9270741`** (tip locale = tip remote, verificato via `git fetch`). Commit chiave: `52738ed` M2-foundation · `2d90af6` endpoint · `9049c13` UI · `d84b04f` `.python-version` · `c3b31ee`/`03fcf0b`/`9e46539` handoff-automate + correzioni · `3f0617c` **Inizializza** · `9270741` tracker · `bd409ef` Q2 re-verify (3° scenario dormiente) · `2e2ed1b` handoff-deploy-check-server. In attesa SERVER (v. Current state sopra). Residuo non tracciato: `.claude/settings.local.json` (permessi locali, da non committare) + `.claude/settings.json` (output-style condiviso — valutare se trackarlo).

---

## Milestone dettaglio & log

*(Dettaglio storico e note di esecuzione qui sotto; la sezione Current state va RISCRITTA a ogni fine sessione, mai appesa.)*

### M1a-lib — 8 fix da portare (con test)
1. Magazzino cleanup saltato su path ID (`sync.py:868-892`).
2. DRY_RUN fail-closed (`main.py:74`): unset/non-riconosciuto → DRY-RUN; muta solo con token APPLY in allowlist.
3. `userErrors` silenziati (`sync.py:562`, `465`) → raise.
4. Fallback prezzo `0.00/0.00` → blocca.
5. Size-matching su "Size" + surfacing.
6. Tag `outlet` + backfill (gated, non M1a) — se adottato, publish SETTA il tag e non azzera gli altri.
7. Images legacy → `productSet`/`productUpdate` (branch batch/REST-fallback DROPPED).
8. Batching >250 (F2): attesa `job.done` per-batch + ABORT su timeout/errore.
