# Pinned to avoid build-from-source on Render with Python 3.12 (runtime.txt provided).
pandas==2.2.3,<3.0
requests>=2.31.0
python-slugify>=8.0.4
tenacity>=8.2.3
gspread>=6.0.0
google-auth>=2.28.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
pytest>=8.0.0

# --- Scansia Manager web foundation (M-foundation) -------------------------
# FLAG for SERVER: verify these BUILD on the Render deploy target (Python 3.12.4
# / runtime.txt). fastapi/uvicorn/httpx themselves are pure-Python wheels; the
# only binary dep is pydantic-core (transitive via fastapi->pydantic v2), which
# ships cp312 manylinux wheels — confirm the wheel resolves and no source build
# is triggered on Render before flipping the cron service to a web service.
fastapi>=0.115,<0.116
uvicorn>=0.32,<0.33     # PLAIN uvicorn (no [standard] extra: no uvloop/httptools build)
httpx>=0.27,<0.28       # TestClient transport for the FastAPI dependency tests
